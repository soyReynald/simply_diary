<?php
require_once(__DIR__ . "/../API/private/conexion.php");


echo "require function WORKING";

/*
$text_diary = $_POST['text_diary'];
$title = $_POST['text_title'];
$user_name = $_SESSION['name'];


$sql = "INSERT INTO sample_text_diary (``, ``, ``) 
VALUES (`{$text_diary}`, `{$title}`, `{$user_name}`)";

if ($conexion->query($sql) === TRUE) {
    echo "Data inserted";
    // we then refresh
} else {
    die("Error".$conexion->error_reporting);
}

$conexion->close();

*/
?>