--> What, when, how, why.

## WHAT would be sample: To fix the LOGIN security [WHAT].
## WHEN would be sample: To check function and method that makes the LOG-IN [WHEN].

To-do:
1. THE script needed in a file external from index.php & WHY: Because is a good practice to have the script externally.
2. On line 157 to check if we can FIX and change the approach from href (GET) to POST (which means changing the form SENT method).
3. Function on line 89, is to be fix and add the formulary approach to POST the data (this is DEPENDING on the second above).
4. On line 40, from php_statements: make this function and CLASS above more secure with the security standards.